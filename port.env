# port.env: the port's knobs, read by scripts/port.sh (see port.sh --help)
ORIGINAL="python3 legacy/ORIGINAL.py"
THREADS=4
SWITCH=X_SPEC=1
PROBE="<hot args>"
HOT="<hot args>"
PIN="<original commit, toolchain, flags, threads>"
MAIN=port/main.bend
CASES=goldens/cases.tsv
GOLDENS=goldens
QUIET=0
