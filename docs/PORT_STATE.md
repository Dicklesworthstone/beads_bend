# Port state — BEADS_RUST → Bend 2

<!-- Read FIRST on every resume; rewritten at the END of every session.
     Facts only, each with the command that produced it. No "should",
     no "later". -->

## Where we are

| field | value |
|---|---|
| phase | 3 reference port, at its exit gate, with Phase 4 find-fix rounds running. Done: −1 fit screen; 0 truth pack (1094 goldens; the 27 cases added in this session's last part floored STABLE; the two UNSTABLE cases are DISC-010 and DISC-011); 1 spec (909 clauses, spec-lint 0 findings); 2 architecture (every clause homed). The port: 31 modules under `port/core/` and the IO shell `port/main.bend` (custom effects `Sys.exit`, `Sys.cwd`, `Sys.remove`, `Sys.stdin`, `Clock.wall`). **1094 of 1094 cases pass on c-1t, c-8t and js, one build each of this commit's `port/`**; `prefix_config_both` (DISC-010) and `delete_hard_cascade_json` (DISC-011) pass on this capture only because the original's random draw fell on the value the port prints (`ii-gv0` this time, `pp-gv0` at the capture before); another capture can fail them again, and both DISCs stay OPEN. The interpreter lane passed a 43-case sample on this code (row below), not the corpus |
| tier | T3 |
| bend | `bend 2.0.20` (release binary, sha256 `fab9e564c578a0a1…`; run through `scripts/bend-cli.sh`); drift vs the skills' 2.0.16: DRIFT, itemized in PLAN §2b and §8 |
| original | `br 0.6.0`, tag `v0.6.0`, commit `b1cfebe`, release binary sha256 `21b967c1ae68df1a…`, run as `scripts/ws-run.sh --oracle br --no-db ::` |
| last updated | 2026-09-24 by Claude (Opus 5.5, 1M context), session 8 |
| remote | `origin` = https://github.com/Dicklesworthstone/beads_bend (public); sessions end with `main` pushed. Open work is in the repository's own `.beads/` workspace (prefix `bb`, `br ready --json`); `docs/BACKLOG.md` section H is the bridge plan the beads came from |

Every command below assumes: `cd /data/projects/beads_bend; export BEND_NO_TELEMETRY=1 BEND_CLI=$PWD/scripts/bend-cli.sh LANE_WRAP=$PWD/scripts/ws-run.sh BUN_JSC_forceRAMSize=10737418240`

**Memory on the 30 GB host, shared with other sessions.** `BUN_JSC_forceRAMSize=10737418240` is what makes the gates fit. Measured this session: `PROOF.bend` 11.0–12.8 GB, 316–384 s; the JS build 13.8–16.5 GB, 128–244 s; the C build in two steps, emission `bend main.bend -o <out>/bn.c` 19.0–23.6 GB, 268–302 s, then `clang -std=c11 -O3 <out>/bn.c -lpthread -lm -o <out>/bn` 5.7–6.0 GB, 331–358 s. Announce an emission to the other sessions on the host before starting it. A JS-lane run of the port on a 25 MB store reached 21 GB and was stopped (bead `bb-rvt`).

## Last gate outputs (paste, do not paraphrase)

| gate | command | result | date |
|---|---|---|---|
| c-1t | `./scripts/conform.sh goldens/cases.tsv goldens --lane c-1t --timeout 120 -- scripts/ws-run.sh <out>/bn --threads 1 --gpu off -- ::` (binary: the two-step build above of this commit's `port/`). Every lane row pastes conform's last line without its `cases` array. `prefix_config_both` (DISC-010) and `delete_hard_cascade_json` (DISC-011) pass on this capture only because the original's random draw fell on the value the port prints (`ii-gv0` this time, `pp-gv0` at the capture before); another capture can fail them again, and both DISCs stay OPEN | `{"lane": "c-1t", "passed": 1094, "failed": 0, "inconclusive": 0, "stderr_compared": true, "verdict": "PASS", "oracle_identity_checked": true}` | 2026-09-24 |
| c-8t | the same binary, `--lane c-8t --timeout 120 … --threads 8` | `{"lane": "c-8t", "passed": 1094, "failed": 0, "inconclusive": 0, "stderr_compared": true, "verdict": "PASS", "oracle_identity_checked": true}` | 2026-09-24 |
| js | `./scripts/conform.sh goldens/cases.tsv goldens --lane js --timeout 120 -- scripts/ws-run.sh python3 $PWD/scripts/js-lane.py <out>/bn.js -- ::` (bundle of the same tree) | `{"lane": "js", "passed": 1094, "failed": 0, "inconclusive": 0, "stderr_compared": true, "verdict": "PASS", "oracle_identity_checked": true}` | 2026-09-24 |
| kill-switch | the js conform above with `scripts/ws-run.sh env BEADS_RUST_SPEC=1 python3 …` (the spec twins: `Store.load`, the list scans of the BLOCKED relation and its users, the record scan of the import check), the bundle of `fd0a945`, on the 1009-case corpus of that commit (not re-run on the round-12 repairs); the same two failures as without the switch there | `{"lane": "js", "passed": 1007, "failed": 2, "inconclusive": 0, "stderr_compared": true, "verdict": "FAIL", "oracle_identity_checked": true}` | 2026-09-24 |
| interpreter | `scripts/interp-sample.sh` on `02393f5` (43 cases, one per case prefix; each single-step row with `@env=BUN_JSC_forceRAMSize=10737418240` and `@env=INTERP_NOTE_CACHE=1`, a scenario row without them, since `ws_inner.py` applies `@env` to one step only; bead bb-oxc), then its scenario row alone after that fix | PARTIAL: a 43-case sample, not the corpus: `{"lane": "interpreter", "passed": 42, "failed": 0, "inconclusive": 1, "stderr_compared": true, "verdict": "INCONCLUSIVE", "oracle_identity_checked": true}` (the one: `scn_lifecycle`, refused by the sandbox because the script prefixed `@env` to a scenario) and `{"lane": "interpreter", "passed": 1, "failed": 0, "inconclusive": 0, "stderr_compared": true, "verdict": "PASS", "oracle_identity_checked": true}` (`scn_lifecycle` alone): 43 of 43, ~5 min and up to 14 GB per step | 2026-09-24 |
| lanes | `LANE_WRAP=$PWD/scripts/ws-run.sh ./scripts/lanes.sh goldens/cases.tsv goldens $PWD/port/main.bend --threads 8 --timeout 120 --interpreter-timeout 600` | BLOCKER: not run. `lanes.sh` builds in one step (19 GB held while clang runs) and runs the interpreter over the corpus; the three fast lanes were run lane by lane instead | 2026-09-24 |
| proofs | `(cd port && $BEND_CLI PROOF.bend)` on this commit's `port/` | `All terms check.` (0 @unsafe; 73 laws; bend 2.0.20; 12.5 GB peak, 529 s) | 2026-09-24 |
| law coverage | `./scripts/law-coverage.sh` | `{"laws": 73, "proofs": 73, "unproved": "", "ghost_proofs": "", "ghost_cited": "", "uncited": "blocked_relation_table_hierarchy blocked_set_member blocked_set_nonmember blocked_table_lookup blocked_table_lookup_missing fast_is_spec store_load_fast_dangling store_load_fast_duplicate work_blocked_rows_hierarchy work_blocked_rows_table work_figures_table work_lead_subsecond work_ready_table", "duplicate_laws": [], "duplicate_proofs": [], "unsafe": 0, "unsafe_annotations": 0, "verdict": "OK"}` | 2026-09-24 |
| spec | `python3 -B scripts/spec-lint.py docs/EXISTING_BEADS_RUST_STRUCTURE.md goldens/cases.tsv` | PASS: `spec-lint: 909 clauses, 1094 cases, 1094 cases cited, 0 finding(s)` | 2026-09-24 |
| architecture | `python3 -B scripts/arch-lint.py` (the one finding: `LAWS.bend still carries the template's S0.1 / core_fast law`, owner question below) | `{"clauses": 774, "homed": 774, "numeric_rows": 25, "s6_rows": 36, "fast_twins": 2, "missing": [], "findings": 1, "verdict": "FAIL"}` | 2026-09-24 |
| parity | `./scripts/parity-board.sh docs/FEATURE_PARITY.md` | `{"rows": 42, "present": 9, "partial": 20, "missing": 0, "excluded": 13, "na": 0, "no_evidence": 0, "verdict": "PARTIAL"}` | 2026-09-24 |
| floor | `./scripts/floor.sh <new>.tsv goldens --repeat 3 -- scripts/ws-run.sh --oracle br --no-db ::` over the 27 cases this part added (before them this session: 18, 35, 16 and 5 cases, each STABLE) | `{"repeat":3,"stable":27,"unstable":[],"inconclusive":[],"oracle_identity_checked":true,"verdict":"STABLE"}` | 2026-09-24 |
| cases | `./scripts/cases-lint.sh goldens/cases.tsv` | `{"cases": 1094, "errors": 0, "notes": 13, "classes_missing": "", "verdict": "OK"}` | 2026-09-24 |
| port lint | `python3 -B scripts/port-lint.py port/main.bend port/core/*.bend --laws port/LAWS.bend --json-only` (the one error: PL-17 on the template law, an owner question; the third PL-01 is `joined_go`'s `x ++ (sep ++ acc)`, which walks one piece and the separator, never the accumulator) | `{"files": 32, "findings": 318, "errors": 1, "warnings": 5, "infos": 312, "by_rule": {"PL-02": 312, "PL-01": 3, "PL-04": 1, "PL-12": 1, "PL-17": 1}, "laws": "port/LAWS.bend", "spec": "…/docs/EXISTING_BEADS_RUST_STRUCTURE.md", "verdict": "FINDINGS"}` | 2026-09-24 |
| pin | `./scripts/pin-check.sh docs/PIN.toml`, after a bead edit (bb-nvc fixed: the capture no longer records the repository's own `.beads/` files) | `{"schema":"p2b.pin-check.v1","sha":"a1b64b8","bend":"bend 2.0.20","host":"Linux-x86_64","checks":[{"check":"original_present","status":"GREEN","note":"legacy/BEADS_RUST_v0.6.0"},{"check":"original_commit","status":"YELLOW","note":"no VCS identity; preserve original source hashes with the capture"},{"check":"original_gitignored","status":"GREEN","note":"git check-ignore: legacy/BEADS_RUST_v0.6.0"},{"check":"original_version","status":"GREEN","note":"br 0.6.0"},{"check":"manifest","status":"GREEN","note":"captured: 2026-09-24T14:41:44.309062+00:00"},{"check":"manifest_hashes","status":"GREEN","note":"3201 hashes and capture inputs verified"},{"check":"manifest_command","status":"GREEN","note":"[\"scripts/ws-run.sh\", \"--oracle\", \"br\", \"--no-db\", \"::\"]"},{"check":"manifest_version","status":"YELLOW","note":"MANIFEST version lacks the pinned version string 'br 0.6.0' (the original does not answer --version, or the pin moved)"},{"check":"case_count","status":"GREEN","note":"1067 cases = 1067 goldens = MANIFEST 1067"},{"check":"bend_version","status":"GREEN","note":"bend 2.0.20"},{"check":"lane_interpreter","status":"GREEN","note":"/data/projects/beads_bend/scripts/bend-cli.sh"},{"check":"lane_c-1t","status":"GREEN","note":"Ubuntu clang version 21.1.8 (6ubuntu1)"},{"check":"lane_c-Nt","status":"GREEN","note":"Ubuntu clang version 21.1.8 (6ubuntu1)"},{"check":"lane_js","status":"GREEN","note":"bun 1.4.2"},{"check":"approver","status":"GREEN","note":"Jeffrey Emanuel (repository owner)"}],"verdict":"YELLOW"}` | 2026-09-24 |
| incumbent | `./scripts/incumbent-bench.sh --pin …` | NOT_RUN: Phase 5 has not begun (the parity gate comes first) | |

**The real-store sweep (a probe, not a gate).** `scripts/real-store-sweep.py run <dir> <out>/bn -j 3` runs `count`, `list --json`, `ready --json`, `blocked --json`, `stats --json` and `show <first id> --json` on the original and on the port over snapshots of the host's `.beads/issues.jsonl` stores, each copied in through `scripts/ws-run.sh … :: @store=<path>` (`/dp/beads_rust` excluded); runs 1 and 2 ran the same code from the session's scratch directory before it was committed as this script. Run 1, binary of `d3f9239`: `{"stores": 138, "runs": 828, "differences": 34, "stores_with_difference": 13}`. Run 2, binary of `fd0a945`: `{"stores": 138, "runs": 828, "differences": 16, "stores_with_difference": 5}`; all 16 are DISC-012 (both sides refuse, exit 6, `SYNC_CONFLICT`; only the named record differs), in the 5 stores with several refused records. Run 3, binary of this commit: `{"stores": 138, "runs": 828, "differences": 16, "stores_with_difference": 5}`; every difference is DISC-012 (both sides refuse, exit 6, `SYNC_CONFLICT`, only the named record differs), in the same 5 stores as run 2; the stored-metadata rule refused no real store (none holds a non-JSON `metadata`).

Reading the table. The three fast lanes are the whole corpus on one build each. The sweep is a probe, not goldens: the stores are other projects' trackers and are not committed. Its run 1 found three behaviors no case had captured (round 11 below); each is now a set of captured cases and a repair. What run 2 still reports is DISC-012: with several refused records the original names one at random (for the same store it named `bd-38oh` in run 1 and `bd-8po2` in run 2).

What the port does NOT do, and answers with its own `INTERNAL_ERROR` failure (exit 1) instead of guessing: `--agent-context @path`; an `--agent-context` float beyond 15 significant digits or outside ryu's plain notation, a malformed JSON literal, a repeated key; a stored dependency `metadata` that is JSON the port cannot reprint exactly (none among the 190,307 set `metadata` values of the 138 real stores); `stats --activity` (needs git, PLAN §3); `dep cycles` on a graph that still has a cycle (OQ-129); the help text of the levels no case prints.

## What this session added

Session 8 (2026-09-24), the beads of the 2026-09-23 reality check taken in order.

- **Performance levers, each a fast twin behind `BEADS_RUST_SPEC=1` with closed laws** (`perf/EXPERIMENTS.md`, exploratory timings, nothing measured): EXP-003 the load's duplicate-id check through a `Map` (quadratic → linear: 4,000 records 32.9 s on C before); EXP-004 the BLOCKED relation's record lookups and the id sets of `ready`/`stats`/`blocked` through `Map`s; EXP-005 the relation itself over a table (a 2,945-record store with 7,818 edges: `stats --json` 43.7 s → 15.4 s, single runs).
- **The real-store sweep** (bead `bb-acg`): `ws_inner.py` gains `@store=<absolute path>` (re-captured with `--repin`: no golden hash moved except the two documented draws), 138 store snapshots, run 1 above.
- **Three behaviors found by the sweep, captured and repaired**: the import's dependency edges (S2.57 amended, S2.58 new: a target that is not stored, an edge on itself, an `issue_id` naming another record; 13 cases, OQ-131); `show` of a missing target (`[missing issue: <id>]`, OQ-132, 2 cases); lead times keep fractions of a second (OQ-133, 1 case). DISC-012 (OPEN) records the original's random pick among several refused records.
- **JS lane**: a text member of 64 KB or more faulted the JS lane (DISC-008 amended, bead `bb-rvt`): `Json.codes`, `Json.byte_len`, `Store.has_nul`, `Store.has_space` recursed once per character; with accumulators the load answers a 1.4-million-character member (probe `port/probes/longstr/`).
- **Round 12** (non-author, bead `bb-9ru`): 35 `r12_*` cases; 5 behaviors in 8 of them repaired (a third S2.58 refusal for a `parent-child` edge to `external:`, ephemeral records skip the import checks, the `external:` end's title and sort; OQ-134).
- **Harness modes** (bead `bb-sfm`): `ws_inner.py` gains `@env=NAME=VALUE`, `@cwd=<dir>`, `@ls`, `@fx=nostore` and a `.redirect` sidecar; 18 cases; OQ-008, OQ-011, OQ-014, OQ-084, OQ-085, OQ-086, OQ-087, OQ-103 and half of OQ-023 resolved (21 → 13 open); two repairs: `BR_OUTPUT_FORMAT=json` selects JSON (S1.6), an absent `BEADS_DIR` is CONFIG_ERROR exit 7 (S1.57).
- **Markdown import** (bead `bb-y4p`, OQ-051): `create -f <file>.md` is ported for headings, the first-line description and the description, design, acceptance criteria, assignee, type, priority and labels sections, with the per-heading priority refusal; a file with `### Parent` or `### Dependencies` keeps the port's refusal. Harness `@file=<name>`. S4.237 corrected (the description is the first line, not the paragraph). 5 cases.
- **Harness fixes**: `bb-nvc` fixed at its source (the skill's `case_manifest.py`); `interp-lane.sh` re-copied (note cache).
- **Markdown import, second half** (bead `bb-y4p`, OQ-135): `### Parent` (an earlier heading's title or an id makes a dotted child; a title of a heading further down defers a parent-child edge) and `### Dependencies` (words by title or id, `type:ref`, unresolved words and cycles warn and exit 5, the cycle path from the least id); 8 cases.
- **JS lane on long texts** (bead `bb-rvt`): the decoders read a comment or an edge member with the one reader its index needs; `show`'s wrap splits and joins by accumulators; the folds of `search` and the control-character escape of a body are accumulators; comments keep trailing blanks (only line feeds are stripped). Fixtures `long_texts` (80 KB description, 40 KB notes, 70 KB comment, 70 KB metadata) and `comment_trailing_ws`; 5 cases.
- **OQ-136** (found building `long_texts`): the import's metadata rule (S2.58), Rust `{:?}` quoting with the `\u{…}` set measured over every scalar value (`Failure.hidden_table`, 907 ranges), `line L column C` positions, and three lexer corrections; 14 cases; bead `bb-04n` for the one ordering not reproduced.
- **Interpreter sample** (bead `bb-oxc`): run 12:18–12:53 under memory pressure from other sessions: 5 pass, 37 fail, 1 inconclusive, every failure an immediate exit 1 (a case takes ~6.5 min); `empty_list` re-run alone passed (395 s, 14.3 GB). Not a port verdict; re-run on this commit is the next action.
- **Laws** 61 → 73 (five of them name the BLOCKED relation's table twin and its id table and set; port-lint PL-11 29 → 0). **Corpus** 993 → 1094. AGENTS.md's facts brought up to date (bead `bb-nr7`), README too (`bb-a16`).

## Open items

| id | what | blocks | owner |
|---|---|---|---|
| DISC-009, DISC-010, DISC-011, DISC-012 | all four OPEN, each with its measured impact: the wall clock's precision per engine; `issue_prefix` vs `prefix` drawn at random by the original; `events_removed` of a cascaded purge drawn at random; which refused record the import names when several fail | convergence (an OPEN DISC blocks it) | **the repository owner: accept, reject, or choose a canonicalizing wrapper** (bead `bb-mjf`) |
| scaffold law | `LAWS.bend`'s template law `fast_is_spec` (S0.1, `core/scaffold.bend`) binds nothing of this port; arch-lint FAIL and port-lint PL-17 both name it. LAWS.bend is human-owned | arch-lint, port-lint | **the repository owner** (bead `bb-ym9`) |
| `bb-04n` | a store line whose object lacks a required field AND has trailing characters: the original names the missing field (serde checks the fields when the object closes), the port names the trailing characters (OQ-136) | a corner of S2.27 no case captures yet | author |
| `bb-rvt` | the JS lane on the real 25 MB and 46 MB stores after this part's repairs: not re-run (21 GB; the host was shared all day) | the JS half of the large-store claim | author |
| OQ register | 136 rows, 12 open (`scripts/converge.sh`): OQ-004, OQ-005 (`init` under `--no-db`, bead `bb-sew`), OQ-012, the `redirect` half of OQ-023, OQ-042, OQ-074, OQ-081, OQ-089, OQ-091 (a closed-pipe harness mode), OQ-100, OQ-106, OQ-129 (owner, bead `bb-ptx`) | phase 4 convergence | author, owner |
| interpreter lane | a 43-case sample passed on `02393f5` (row above); the whole corpus (1094 cases, ~5 min each, ~90 h) has not run | T3 "every lane" | author (bead `bb-oxc`) |

## Find-fix rounds (Phase 4/5)

| round | lens | new genuine findings | fixed | clean | date |
|---|---|---|---|---|---|
| 1 | the spec clauses the corpus does not reach, over the five commands session 3 ported (author round) | 1: S4.341 — a second `delete` of an already-tombstoned id rewrote four fields and the store; the original writes nothing | 1 (`core/remove.bend`) | no | 2026-09-20 |
| 2 | the S9 error table against the port's failures (author round) | 2: S9.22 — the `SELF_DEPENDENCY` context key is `id`, not `issue_id`; S9.11 — the shell's `String.trim_end` ate a trailing SPACE | 2 (`core/edges.bend`; `port/main.bend`) | no | 2026-09-20 |
| 3 | the JSON envelopes and payload rows of the six commands session 3 ported (author round) | 0: six captured envelopes matched the port's spec-derived text byte for byte | 0 | yes | 2026-09-20 |
| 4 | Quiet mode over the mutations (S5.106, S5.107, S9.26, OQ-015) (author round) | 2: `close --quiet` still printed the per-item `Warning: Skipped …` lines; `reopen --quiet` printed nothing where the original prints `✓ Reopened` | 2 (`core/run.bend`) | no | 2026-09-20 |
| 5 | the actor ladders (S4.200, S4.201) (author round) | 0: `actor_flag_dep_add`, `actor_flag_delete` and `actor_flag_label_add` matched on their first run | 0 | yes | 2026-09-20 |
| 6 | the dependency graph beyond depth 0 (author round) | 4: S5.179 precedence; S4.184's sort keys; the original's mixed-pair cycle gap (OQ-129); the DISC register's `Resolution` field shape | 4 | no | 2026-09-21 |
| 7 | `dep list --direction up` and `--direction both` (author round) | 0: ten captured cases matched on their first run | 0 | yes | 2026-09-22 |
| 8 | re-test the last repairs: the forms closed in sessions 5–6 crossed with flags their cases did not combine (author round) | 0: thirteen captured cases (`r8_*`, `scn_r8_*`), floored STABLE, matched on js on their first run | 0 | yes | 2026-09-23 |
| 9 | `--quiet` and the JSON error envelopes over the same closures (author round) | 0: thirteen captured cases (`r9_*`, `scn_r9_*`), floored STABLE, matched on js on their first run | 0 | yes | 2026-09-23 |
| 10 | the query commands over the larger fixtures no query case had used, run by a fresh agent that did not write the port, with only the spec and the harness (non-author) | 0: thirty-five captured cases (`r10_*`), floored STABLE, matched on js on their first run | 0 | yes | 2026-09-23 |
| 11 | real stores: the original and the port on snapshots of 138 `.beads/issues.jsonl` stores found on this host, six read commands each (author round) | 3: the import refuses dependency edges the corpus never had (S2.57, S2.58, OQ-131); `show` of a missing target (S5.151, OQ-132); lead times keep fractions of a second (S4.154, OQ-133) | 3 (`core/store.bend`, `core/show.bend`, `core/work.bend`, `core/model.bend`) | no | 2026-09-24 |
| 12 | the load's import checks, the BLOCKED relation's corners, lead-time edges and `show`'s dependency lines (S2.57–S2.58, S4.151–S4.178, S5.151), run by a fresh agent that did not write the port, with only the spec and the harness (non-author) | 5: in 8 of 35 captured cases (`r12_*`, floored STABLE): a `parent-child` edge to an `external:` target is a third S2.58 refusal; an ephemeral record is dropped before the import checks, so an edge to one dangles (2 cases); an `external:` end's title drops the prefix for `<project>:<id>`; an `external:` end sorts by its edge's `created_at` (OQ-134) | 5 (`core/store.bend`, `core/show.bend`); 35 of 35 on js after the repair | no | 2026-09-24 |
| 13 | long texts on the JS lane (bead `bb-rvt`) and what building their fixture exposed: the import's check of a stored dependency's `metadata`, serde_json's placement and quoting of a refused JSON text (S2.24, S2.27, S2.58, S4.350, S5.115, S5.193) (author round) | 8: in 19 of 27 captured cases: `show` and `search` faulted the JS lane on an 80 KB body (per-character recursion in Base `String.split`/`String.join`, an eager `Bool.pick` in `indent_of`, the folds); a comment body kept only its trailing line feeds stripped, not its trailing blanks; the import refuses a stored `metadata` that is not one JSON value (S2.58, every command but `show`); the refused text is Rust `{:?}`-quoted; the position counts lines; a bad `\u` escape is placed at its fourth byte; a stray character is judged where it stands and stops the lexing; a keyword prefix at the end is `EOF while parsing a value` (OQ-136). Not repaired: a store line's missing field before `trailing characters` (bead `bb-04n`) | 7 (`core/show.bend`, `core/query.bend`, `core/views.bend`, `core/decode.bend`, `core/json.bend`, `core/failure.bend`, `core/store.bend`, `core/edges.bend`, `core/model.bend`); 27 of 27 on js | no | 2026-09-24 |

Convergence (`./scripts/converge.sh docs/PORT_STATE.md`): `NOT_CONVERGED` — round 13 found eight behaviors, so the clean tail restarts; 12 OQs are open and four DISCs are OPEN (`DISC-009` … `DISC-012`), which only the repository owner can accept or reject.

## Next action (one line, executable)

`LANE_WRAP=$PWD/scripts/ws-run.sh ./scripts/lanes.sh goldens/cases.tsv goldens $PWD/port/main.bend --threads 8 --timeout 120 --interpreter-timeout 600` is the gate still owed (the interpreter over all 1094 cases, ~90 h): run it on a host with no other sessions, or ask the owner for a decision on the T3 "every lane" rule for the interpreter; meanwhile bead `bb-04n` (a store line's missing field before trailing characters).
