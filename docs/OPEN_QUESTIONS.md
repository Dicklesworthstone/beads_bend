# Open questions — the Bend 2 port of BEADS_RUST

<!-- A spec gap is never resolved by reading the original's source during
     implementation; it is resolved by RUNNING the original on a new case,
     capturing it as a golden, and amending the spec clause. Each entry
     names the case that resolved it. -->

| id | question | spec clause | resolve by (case to add) | resolution | date |
|---|---|---|---|---|---|
| OQ-001 | The original sorts through SQL `ORDER BY created_at` over TEXT. With mixed fraction widths (`…05Z`, `…05.1Z`, `…05.000000001Z`, an offset zone), is the order textual or chronological, and what breaks ties? | S6 (order), S4 (ready/list sort) | `edge_precision_ready`, `edge_precision_sort_created`, `edge_precision_list_json` (fixture `precision`) | RESOLVED: chronological. `edge_precision_ready.out` lists `proj-aae` (12:00:04+02:00 = 10:00:04Z), `proj-aaa` (`…05Z`), `proj-aac` (`…05.000000001Z`), `proj-aab` (`…05.100Z`), `proj-aad` (`…05.1Z`, equal instant: id ascending breaks the tie), then `aaf`, `aag`, `aah`. `--sort created_at` is newest first with the same id-ascending tie-break (`aah aag aaf aab aad aac aaa aae`) | 2026-09-20 |
| OQ-002 | When a mutation rewrites `issues.jsonl`, what happens to records it did not touch: unknown fields, `""` optionals, duplicate or unsorted labels, non-canonical timestamps, absent defaults? | S2 (load), S5 (write-back) | `edge_precision_rewrite` | RESOLVED from `edge_precision_rewrite.out`: every record is rewritten. `unknown_field` is dropped. `"description":""` and `"assignee":""` are dropped, while `"compacted_at_commit":""` is kept. Labels come back sorted and deduplicated (`["alpha","zeta"]`). `…05.1Z` becomes `…05.100Z`; `12:00:04+02:00` becomes `10:00:04Z`. Records without them gain `"source_repo":"."`, `"compaction_level":0`, `"original_size":0`. Which other optionals behave like `description` versus like `compacted_at_commit` is OQ-007 | 2026-09-20 |
| OQ-003 | `br version` prints the original's build metadata (`0.6.0`, commit `b1cfebe…`, rustc). The port cannot truthfully print those bytes. What is the port's `version` contract? | S1 | (case to add: `version_plain`, `version_json`) | RESOLVED: both cases are captured (`br version 0.6.0 (release) (v0.6.0@b1cfebe)` and the seven-member JSON object), and the port's contract is DISC-006 (ACCEPTED): its own name `bn`, its own fields, compared through a shape canonicalizer once `version` is ported | 2026-09-20 |
| OQ-004 | What precision does Bend's `now` effect deliver on each lane (the original prints nanoseconds, AutoSi 0/3/6/9 digits)? | S7, S8 | read the host functions of the pinned Bend: `~/.bend/bend2/effs/now.c`, `now.js`, and `io_tick` in `toolchain/bend-v2.0.20-src/bend2/comp.ts:5383` | RESOLVED, and worse than a precision gap: Bend 2.0.20 has NO wall clock. `IO.now() -> IO(Nat)` is `io_tick() / 1000000` with `io_tick` = `clock_gettime(CLOCK_MONOTONIC)` on the C lane (milliseconds since boot) and `BigInt(Math.floor(performance.now()))` on the JS lane (milliseconds since process start); `rg 'CLOCK_REALTIME|gettimeofday|Date\.now'` over `comp.ts` and `effs/` finds only the audio effects. Consequence for Phase 2: the shell needs a custom effect pair (`.c` with `CLOCK_REALTIME`, `.js` with `Date`/`performance.timeOrigin`), returning seconds and nanoseconds separately because epoch nanoseconds (≈1.8e18) exceed `Nat`'s 2^48−1; its precision on the JS lane and whether custom effects load on the interpreter lane are the probe that follows (OQ-009) | 2026-09-20 |
| OQ-005 | What does `br --no-db init` do (the default `init` creates `beads.db`, which the port cannot)? Which files, which messages, and what when `.beads/` exists? | S1, S8 | (case to add: `init_fresh` on `@fx=none`, `init_existing`) — needs ws-run to dump every file under `.beads/`, not only the store | OPEN | |
| OQ-006 | Id length grows with the issue count (birthday bound; 3 characters up to a threshold near 163 issues, then 4, …). At which exact counts does the length step, in `--no-db` mode? | S4 (id generation), S7 | (case to add: `edge_idlen_163`, `edge_idlen_164` on generated fixtures `n163`, `n164`) | OPEN: generate the fixtures through the oracle, capture, then state the table | |
| OQ-007 | For each optional string field, does an empty string survive a rewrite (as `compacted_at_commit` does) or vanish (as `description` and `assignee` do)? | S2, S5 | (case to add: `edge_empty_optionals_rewrite` on a fixture with every optional set to `""`) | RESOLVED by extractor S2+S5a running the original (clause S5.9): `compacted_at_commit` is the ONLY optional string that survives a rewrite as `""`; every other one is read back through an accessor that maps `""` to absent, and `external_ref` loses it earlier, to a trim. The all-optionals fixture case stays a (case to add) under S5.9 | 2026-09-20 |
| OQ-008 | The sidecars: when exactly is `.beads/last-touched` written and what are its bytes (trailing newline?), and do read-only commands ever create files? | S8 | `create_min`, `update_status`, `scn_last_touched` already capture the dump; (case to add: a read-only case under a ws-run mode that lists `.beads/`) | OPEN | |
| OQ-009 | Does a custom effect (a `def` whose body imports `./x.c` and `./x.js`) load on all three engines of the pinned Bend (interpreter, C, JS), and what sub-millisecond precision does the JS side deliver? | S8 | `port/probes/clock/` (`main.bend`, `clock_wall.c`, `clock_wall.js`): `Clock.wall() -> IO(String)` answering `<epoch seconds>.<nine digits>` | RESOLVED 2026-09-20 under bend 2.0.20: it loads on all three. interpreter `1789884581.715000000`, C `1789884582.378291177`, JS `1789884582.638000000` (`date +%s.%N` said `1789884582.646053126`). C is nanosecond (`CLOCK_REALTIME`); interpreter and JS are millisecond (`Date.now()`), so an unpinned timestamp prints 9 fraction digits on the C lane and 3 on the JS lanes: a DISC (class `Platform`) when the clock lands. Two drift facts seen in the same run: (1) a program that reaches a foreign def gets the verdict `All terms check, but 2 defs rely on unsafe or foreign code:` followed by one `- <def>` line each (`- Clock.wall`, `- main`), on every engine, so the pure core and the effectful shell live in separate files and `PROOF.bend` imports only the core; (2) that note is now several lines, while `scripts/interp-lane.sh` was written for 2.0.16's one-line note: to be verified on the first real lane run in Phase 3 | 2026-09-20 |
| OQ-010 | What are the exact bytes and exit code of `br version` and `br version --json` for the port, given that the commit, rust version and target cannot be truthfully reproduced? (OQ-003, still open) | S9.33 | `version_plain`, `version_json` (listed in cases.tsv; capture them, then a DISC of class Platform states the port's fields) | OPEN (proposed by extractor `S1_S9`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-011 | What happens when `.beads/` exists but `issues.jsonl` does not? Is it an empty store (exit 0) or `NOT_INITIALIZED`/an I/O error? | S9.30, S9.31 | `edge_no_jsonl_list` (a fixture selector that creates `.beads/` with no `issues.jsonl`) | OPEN (proposed by extractor `S1_S9`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-012 | Does an argument that is not valid UTF-8 abort inside the argument reader, and with what bytes and exit code (probe RS-20)? | S1.2 | `usage_non_utf8_arg` — argv `["\xff"]` | OPEN (proposed by extractor `S1_S9`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-013 | Is the `CLOSE_INCOMPLETE` context really `{"closed","skipped","summary"}`, and in that key order? Only the human rendering was measured. | S9.14 | `error_close_partial_json` — `["@fx=basic","close","proj-mta","proj-mkh","--json"]` | OPEN (proposed by extractor `S1_S9`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-014 | Does a clap parse failure with `BR_OUTPUT_FORMAT=json` set still print plain text on stderr, as it does with `--json`? | S1.30, S9.4 | `usage_limit_bad_env_json` (needs a ws-run mode that sets one environment variable) | OPEN (proposed by extractor `S1_S9`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-015 | Does `--quiet` suppress the per-item `Warning: Skipped …` lines of S9.26, or only the command's stdout? | S1.8, S9.26 | `close_blocked_quiet` — `["@fx=basic","close","proj-170","--quiet"]` | RESOLVED by running the original (lint-fix pass, 2026-09-20): `--quiet` DOES suppress the per-item `Warning:` lines (`src/output/context.rs:1278`); clause S9.26 amended. The proposed case stays a (case to add) so the bytes are pinned by a golden | 2026-09-20 |
| OQ-016 | What are the exact bytes when more than five conflict markers are present (the preview is capped at five)? | S9.23 | `error_conflict_markers_many` (a fixture with six or more markers) | OPEN (proposed by extractor `S1_S9`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-017 | Does `--actor ""` fall through to the configuration and `USER`, or is the empty string taken as the actor? | S1.64 | `actor_flag_empty` — `["create","X","--actor","","--json"]` | OPEN (proposed by extractor `S1_S9`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-018 | OQ-007 is answered by S5.9: `compacted_at_commit` is the only optional string that survives a rewrite as `""`. Confirm against the original rather than against the accessor table | S5.9, S2.38 | `edge_empty_optionals_rewrite` (a fixture with every optional string set to `""`) | OPEN (proposed by extractor `S2_S5a`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-019 | Does a UTF-8 BOM refuse the load, and with which line number and column? | S2.25 | `error_jsonl_bom` (fixture `bom`) | OPEN (proposed by extractor `S2_S5a`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-020 | Does a 6-fraction-digit instant exist in the timestamp ladder, i.e. is a microsecond written with exactly six digits? | S5.12 | `edge_precision_micros` (a fixture line with `…05.000001Z`) | OPEN (proposed by extractor `S2_S5a`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-021 | Does a second line with the same `id` raise the duplicate message on the import path as well as on the `show` fast path, and with which line number? | S2.40 | `error_jsonl_duplicate_id` | OPEN (proposed by extractor `S2_S5a`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-022 | Is a custom `status` really written back lower-cased, does `inprogress` normalize to `in_progress`, and does `done` really fold to `closed` and gain a `closed_at`? | S2.31, S2.36 | `edge_status_spellings` (a fixture with `InProgress`, `inprogress`, `QaReview`, `done`) | OPEN (proposed by extractor `S2_S5a`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-023 | Does `BEADS_DIR` bypass the walk, and does a `redirect` file relocate the workspace? Both need a ws-run mode that plants a sidecar or adds one environment variable | S2.4, S2.5 | `env_beads_dir`, `redirect_basic` | OPEN (proposed by extractor `S2_S5a`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-024 | Does a configured `issue_prefix` that sanitizes to empty abort rather than fall back to `br`? | S2.14 | `error_bad_issue_prefix` (a `config.yaml` fixture sidecar) | OPEN (proposed by extractor `S2_S5a`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-025 | Does a record missing `title` produce serde's `missing field` wording inside the `Invalid JSON at line <n>` frame, and does a record that parses but fails validation produce the `Validation failed for issue … at line …` frame? | S2.28, S2.30 | `error_jsonl_missing_title`, `error_jsonl_bad_id_format` | OPEN (proposed by extractor `S2_S5a`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-026 | Does an imported dependency with no `created_by` really come back as `"created_by":"import"`? | S5.14 | `edge_sparse_dependency_rewrite` (a fixture whose dependency carries only the first four members) | OPEN (proposed by extractor `S2_S5a`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-027 | At which exact issue counts does the id hash length step from 3 to 4 to 5? (OQ-006, still open; S4.9 states the computed table) | S4.9 | `edge_idlen_163` — `["@fx=n163","create","Boundary","--json"]`; `edge_idlen_164` — `["@fx=n164","create","Boundary","--json"]` (generated fixtures `n163`, `n164`) | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-028 | Does the count that chooses the hash length include tombstones in `--no-db`? | S4.10 | `edge_idlen_tombstones` — a fixture of 163 live records plus one tombstone, then `create` | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-029 | Does a tombstoned id block a colliding id candidate? | S4.13 | `edge_id_collides_with_tombstone` — a fixture whose tombstone id equals the nonce-0 candidate for a known title/instant | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-030 | After deleting a child, is its number reused by the next child? | S4.25 | `edge_child_after_delete` — `@scn` deleting `proj-uly.1` then creating another child of `proj-uly` | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-031 | What exactly does `--slug` produce, and what does it do when the slug normalizes to empty or exceeds the prefix budget? | S4.19–S4.23 | `create_slug` — `["create","Slugged","--slug","My Thing!","--json"]`; `create_slug_unicode` — `["create","S","--slug","  Survey — my  thing  ","--json"]`; `create_slug_empty` — `["create","S","--slug","!!!","--json"]` | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-032 | What are the bytes for a wrong-prefix partial id and for an uppercase full id? | S4.36, S4.43 | `show_uppercase_id` — `["@fx=basic","show","PROJ-MTA"]`; `error_show_wrong_prefix` — `["@fx=basic","show","other-mta"]` | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-033 | What is the message for a blank id argument? | S4.40 | `error_show_blank_id` — `["@fx=basic","show","  "]` | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-034 | Which RFC 3339 spellings does `--until` accept, and what does `tomorrow` produce under the pinned clock? | S4.57, S4.60 | `defer_until_rfc3339` — `["@fx=basic","defer","proj-mta","--until","2027-06-01T12:00:00+02:00","--json"]`; `defer_until_tomorrow` — `["@fx=basic","defer","proj-mta","--until","tomorrow","--json"]` | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-035 | What is the field name and message for a bad `create --due`? | S4.62 | `error_create_bad_due` — `["create","X","--due","not-a-date"]` | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-036 | What are the messages for out-of-band estimates? | S4.69 | `error_create_negative_estimate` — `["create","X","-e","-1"]`; `error_create_huge_estimate` — `["create","X","-e","525961"]` | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-037 | What do priority ranges, comma lists and a backwards range print? | S4.67 | `list_priority_range` — `["@fx=basic","list","-p","0-1","--json"]`; `list_priority_list` — `["@fx=basic","list","-p","0,3","--json"]`; `error_list_priority_backwards` — `["@fx=basic","list","-p","3-1"]` | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-038 | What does a non-numeric priority report? (the uppercasing oddity of S4.66) | S4.66, S10 | `error_create_priority_word` — `["create","X","-p","high"]` | OPEN (proposed by extractor `S4a_S7`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-039 | Does `--status all` include tombstones in `list`, and in what position do they sort? | S4.104 | `list_status_all` — `["@fx=basic","list","--status","all","--json"]` (needs a fixture with a tombstone: `basic_tomb`) | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-040 | Is a custom status carried by a stored issue accepted by `--status`, and is it matched lowercased? | S4.106 | `list_status_custom` — `["@fx=custom_status","list","--status","Triage","--json"]` | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-041 | Does `:parent-blocked` really reach every descendant, and does a grandchild of a blocked ancestor drop out of `ready`? | S4.159 | `blocked_parent_propagation` — a `hierarchy` fixture with `epic → child → grandchild` plus a `blocks` edge on the epic; run `blocked --json` and `ready` | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-042 | What does a real cycle look like — the witness node order, the numbering, and the exit code? | S4.187, S4.188 | `dep_cycles_found` / `dep_cycles_found_json` on a `cycles` fixture with `a —blocks→ b —blocks→ c —blocks→ a` | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-043 | How does `dep tree` render a diamond (shared node) and a cycle: which occurrence carries `repeat`, which carries `truncated`? | S4.185 | `dep_tree_shared` and `dep_tree_cycle` on a `diamond` / `cycles` fixture | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-044 | Does `--max-depth` cut where S4.185 says, and does the cut node carry `truncated` true? | S4.185 | `dep_tree_max_depth` — `["@fx=hierarchy","dep","tree","<root>","--max-depth","1","--json"]` | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-045 | Does an `external:` blocking edge really block its source with no external project configured, and what does `blocked` print for it? | S4.166 | `blocked_external` on a fixture carrying `proj-mta —blocks→ external:other-1` | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-046 | What is the third empty-`ready` message, verbatim, on a store where every open issue is blocked? | S4.178 | `ready_none_actionable` on a fixture where the only open issue has an open `blocks` prerequisite | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-047 | Are templates really invisible to `list`/`ready`/`count`, and does `count --include-templates` bring them back? | S4.103 | `list_template_hidden` and `count_include_templates` on a fixture with `"is_template":true` | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-048 | Do `--desc-contains`/`--notes-contains` fold non-ASCII case (Unicode lowercasing) while `--title-contains` does not? | S4.129, S4.109 | `list_desc_contains_unicode` — `["@fx=basic","list","--desc-contains","LINE ONE"]` and `["@fx=basic","list","--title-contains","ünï"]` | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-049 | Which sibling key decides first in `dep tree` — priority or status rank? | S4.184 | `dep_tree_siblings` on a fixture where one node has two dependencies differing in priority and in status | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-050 | Does `epic status` list a *deferred* epic (include_deferred true) and hide a closed one? | S4.189 | `epic_status_deferred` on a fixture with a deferred epic and a closed epic | OPEN (proposed by extractor `S4b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-051 | Does `create -f/--file` bulk import write `last-touched` with the last created id, and what are the exact plain and JSON output shapes and the per-issue defaults? | S4.237 | `create_from_file` (a markdown file staged next to the fixture; needs a ws-run mode that can place extra input files) | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-052 | What exactly does `delete --from-file` accept, and does the sorted-id rule change which id is reported first? | S4.340 | `delete_from_file` (same staging need) | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-053 | Does `epic close-eligible` write `last-touched`, and does an eligible epic really receive `close_reason` `All children completed` rather than `done`? | S4.391 | `scn_epic_close_eligible` — a scenario on `@fx=basic`: `["close","proj-170"]`, `["epic","close-eligible","--json"]`, `["epic","status"]` | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-054 | Does `label add <issue> <id-shaped-label>` really treat the last positional as a label, and what happens when a label is id-shaped and `-l` is absent? | S4.370 | `label_add_id_shaped` — `["@fx=basic","label","add","proj-mta","needs-review"]` | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-055 | Does `comments add -f <file>` keep a trailing newline in the stored `text`? | S4.381 | `comments_add_file` (needs a fixture-side file) | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-056 | Is `close`'s `Partially applied` exit code 3 stable for every mix of skip kinds, and does the store always hold the successful closures? | S4.294 | `close_partial_skip` — `["@fx=basic","close","proj-mkh","proj-mta"]` | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-057 | Does `update --status <same non-terminal value>` really rewrite the store on every status, or only on some? | S4.262 | `update_status_same` — `["@fx=basic","update","proj-mta","--status","open"]` | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-058 | What does `--agent-context` store byte-for-byte after JSON re-serialization (key order, number formatting), and what is the verbatim refusal for invalid inline JSON? | S4.232 | `create_agent_context`, `error_create_agent_context` | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-059 | What is the exact normalization of `--slug` at the 48-character cap and for a slug that normalizes to the empty string? | S4.233 | `create_slug`, `create_slug_empty` — `["create","X","--slug","!!!","--json"]` | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-060 | Does `delete --hard` with no ids purge tombstones in a store that has some, and what is the resulting byte content? | S4.339 | `delete_hard_all` on a fixture containing a tombstone | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-061 | Which comment id does a store with NO comments assign, and does the sequence survive a rewrite that renumbers nothing? | S4.382 | `comments_add_first` — `["@fx=empty"…]` is not possible (no issue); a scenario `create` then `comments add` | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-062 | Does `update --parent <id>` replace an existing parent edge or add a second one when the single-parent rule is bypassed by the update path? | S4.270 | `update_reparent` — `["@fx=basic","update","proj-170","--parent","proj-b75","--json"]` | OPEN (proposed by extractor `S4c`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-063 | Is a long title truncated in plain mode when stdout is a pipe? (source says no; no case proves it) | S5.114 | `list_long_title` — `["@fx=longtitle","list"]` on a fixture with a 300-column title | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-064 | At what width does `br show` wrap a free-text body when `COLUMNS` is unset? (source says 100) | S5.115 | `show_wrap_100` — `["@fx=longdesc","show","proj-aaa"]` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-065 | What are the exact bytes of `dep tree` at depth ≥ 1, and of the `(truncated)` / `(shown above)` prefixes? | S5.179 | `dep_tree_children` — `["@scn=child_ids"]` extended with `["dep","tree","proj-uly","--direction","up"]` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-066 | What does a plain `close --suggest-next` print, and is the `Unblocked` block preceded by exactly one blank line? | S5.225 | `close_suggest_next_plain` — `["@fx=basic","close","proj-mta","--suggest-next"]` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-067 | Does a mixed batch (one closed, one skipped) print the `✓` line on stdout and still exit non-zero? | S5.224 | `close_mixed` — `["@fx=basic","close","proj-170","proj-mta"]` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-068 | Does `br reopen --quiet` really print, and does `br create --silent --quiet` print the id? | S5.108 | `reopen_quiet`, `create_silent_quiet` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-069 | What are the bytes of a quiet mutation (stdout, stderr, and the store dump)? | S5.107 | `update_quiet` — `["@fx=basic","update","proj-mta","-p","0","--quiet"]` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-070 | Which stream carries the `list` truncation note and which of the two wordings applies to a plain `--limit`? | S5.124 | `list_limit_plain` — `["@fx=basic","list","--limit","2"]` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-071 | Does the `search` truncation note really land on stdout, and what is the hidden-closed note's exact text? | S5.159, S5.160 | `search_limit`, `search_hidden_closed` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-072 | What is the plain form of `defer --until` — RFC 3339 with `+00:00`, or a date? | S5.230 | `defer_until_plain` — `["@fx=basic","defer","proj-mta","--until","2027-06-01"]` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-073 | What are the plain forms of `undefer`, `label rename`, `update --title`, `update` no-op and `label list` on an unlabeled issue? | S5.232, S5.189, S5.216, S5.218, S5.186 | `undefer_plain`, `label_rename_plain`, `update_title_plain`, `update_noop_plain`, `label_list_empty` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-074 | What does `dep cycles` print when a cycle exists (stdout rows vs the stderr warning)? | S5.182 | `dep_cycles_found` — `["@fx=cycle","dep","cycles"]` on a fixture containing a cycle | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-075 | What does `ready` print when filters exclude everything, versus when everything is blocked (three different `✨` lines)? | S5.133 | `ready_filtered_empty`, `ready_all_blocked_empty` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-076 | What is the countdown suffix on a plain `show` of a deferred issue under the pinned clock? | S5.148 | `show_deferred_plain` — `["@fx=basic","show","proj-7vm"]` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-077 | Does `blocked --limit` really print a header count larger than the row count? | S5.138 | `blocked_limit` — `["@fx=basic","blocked","--limit","1"]` | OPEN (proposed by extractor `S5b`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-078 | `stats --json` prints `average_lead_time_hours` as an f64 in shortest round-trip form. The corpus reaches only the whole value `2.0`. What are the bytes when the mean of the whole-hour lead times is not an integer (e.g. 7/3), and does the plain form print the same digits? | S7.20, NUMERIC_PLAN §3 | (case to add: `stats_lead_time_fraction_json`, `stats_lead_time_fraction` on a fixture `leadtime` built through the oracle: three issues closed after 1 h, 2 h and 4 h) | OPEN: decides whether the port needs the multi-limb binary64 shortest-digits routine; until then the parity row for `stats --json` stays `partial` | |
| OQ-079 | Does the JSONL export order use raw byte order for ids containing `.` (child ids sort `proj-uly` before `proj-uly.1` before `proj-ulz`)? | S6.2 | `edge_child_id_export_order` — a fixture holding `proj-uly`, `proj-uly.1`, `proj-uly.10`, `proj-uly.2`, `proj-ulz`, then any mutation | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-080 | Do `dep list --json` and `dep list` really disagree, and by which key? | S6.25, S10.35 | `edge_dep_list_json_vs_text` — a fixture with three same-priority dependencies whose `created_at` order differs from their id order, run both ways | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-081 | What order does `dep cycles` give when two cycles share a member, and what does `--include-closed` change? | S6.29 | `edge_dep_cycles_two` and `edge_dep_cycles_include_closed` | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-082 | Does `blocked` order by blocker count before or after `created_at` when priorities are equal? | S6.15 | `edge_blocked_count_tiebreak` — two P4 issues blocked by two and one open issues | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-083 | Is the `--sort title` fold ASCII-only for every command, including `search` with an offset (which takes a different code path)? | S6.10, S6.17 | `edge_title_fold_ascii_only` — a fixture with `İstanbul match`, `jackal match`, `hotel match`, `ZULU match`, `apple match`, run as `list --sort title`, `search match --sort title` and `list --sort title --offset 1` | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-084 | Exactly which files does a mutating run leave in `.beads/`, and does a read-only run ever create one? | S8.27, S8.28, S8.29 | a ws-run mode that lists `.beads/` recursively after the step, for both a read-only and a mutating argv (extends OQ-008) | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-085 | What does `BEADS_DIR` do to `where`'s output, and why does the `prefix:` line disappear? | S8.2, S10.49 | `env_beads_dir_where` — needs a ws-run mode that sets one extra environment variable | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-086 | What does `BR_OUTPUT_FORMAT=json` change for a command that already has a `--json` flag, and for one that does not? | S8.7 | `env_output_format_json` on `count` and on `where` | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-087 | Does `BR_DISABLE_PARALLEL_JSONL_EXPORT` change the exported bytes on a large store? | S8.10 | `env_serial_export` on a generated 1000-issue fixture | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-088 | Which per-field rule decides whether an empty optional survives a rewrite? | S10.1 | `edge_empty_optionals_rewrite` (already OQ-007) | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-089 | Does `close`'s `closed_at` differ from `updated_at` under a clock the harness can move between the two readings? | S10.45, S8.31 | none is capturable through the pinned harness; recorded as a clock-seam note for DISC-001 | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-090 | Is the average lead time truncated per issue before averaging, and in which direction? | S6.34, S10.41 | `edge_stats_lead_time_truncation` — three closed issues with lead times of 1 s, 79 h 13 m 11 s and 35 h 22 m 33 s | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-091 | Does a plain-mode closed pipe kill the process with status `141` while a `--json` run exits `0`? | S8.37 | `edge_sigpipe_plain` — needs a harness mode that closes stdout after the first line | OPEN (proposed by extractor `S6_S8_S10`; resolved by adding the case, capturing with `golden-capture.sh --repin`, amending the clause) | |
| OQ-092 | In `--json` mode the original writes the error envelope to stdout, NOTHING to stderr, and exits non-zero (S9.1–S9.5). Bend's only exit path, `IO.die(A, code, msg)`, always writes `msg` to stderr. What does `IO.die(Unit, 3, "")` leave on stderr, and can a custom effect exit silently? | S8.34–S8.36, S9.1–S9.7 | probe `port/probes/exit/` (`main.bend`, `sys_exit.c`, `sys_exit.js`) on all three engines | RESOLVED 2026-09-20 under bend 2.0.20: `IO.die(Unit, 3, "")` → stdout `{"error":1}\n`, stderr `\n` (one stray empty line), exit 3, identically on interpreter, C and JS. A custom `Sys.exit(code)` (C: `fflush` both streams, `exit`; JS: `process.exit`) → the same stdout, EMPTY stderr, exit 3, identically on all three. The shell therefore writes stdout with `IO.write`, stderr with `IO.print_err`/a write, and leaves through `Sys.exit`; `IO.die` is not used | 2026-09-20 |
| OQ-093 | Clause contradiction: S4.313 lists `issue not found` as a `reopen` SKIP reason with exit 0, while S9.9, S4.212 and S5.251 make it an error with exit 3. | S4.313, S9.9, S4.212, S5.251 | `error_reopen_missing` (golden: exit 3 — the reviewer predicted 3 and matched) | OPEN: amend S4.313 to the golden; the contradiction was found by the blind review although the prediction matched (source: `docs/reviews/self-containment-1/NOTES.md`) | |
| OQ-094 | Clause contradiction: S2.56 gives a VALIDATION wording for a missing `dep add` target; S4.353 says target resolution fails first with exit 3. S2.56 does not mark its wording as unreachable on that path. | S2.56, S4.353 | `error_dep_add_missing` (golden matched the S4.353 reading) | OPEN: state in S2.56 on which path, if any, its wording is reachable; run the original to find one or mark it unreachable (source: `docs/reviews/self-containment-1/NOTES.md`) | |
| OQ-095 | `show --json`: the timestamp spelling (`Z` versus `+00:00`) is not stated in S5.30, and that the file's `source_repo`, `source_repo_path`, `original_size` are echoed is only implied. | S5.30 | `show_json` (the reviewer guessed `Z` and matched) | OPEN: add the spelling and the echoed members to S5.30 explicitly (source: `docs/reviews/self-containment-1/NOTES.md`) | |
| OQ-096 | Clause contradiction: S4.195 and S5.30 disagree on whether `show --json` labels are sorted (S6.38 and S10 say they are NOT normalized). | S4.195, S5.30, S6.38 | `edge_precision_show_json` | OPEN: amend the wrong clause to the golden (source: `docs/reviews/self-containment-1/NOTES.md`) | |
| OQ-097 | S5.194 (`comments list` on an issue without comments) and S5.179 (`dep tree`) do not say whether a header line precedes the output. | S5.194, S5.179 | `comments_list_empty`, `dep_tree` (both matched on a guess) | OPEN: state the header rule in both clauses with the golden line cited (source: `docs/reviews/self-containment-1/NOTES.md`) | |
| OQ-098 | Clause contradiction: S1.12 and S5.25 disagree on a repeated `--json`. | S1.12, S5.25 | `usage_json_twice` (golden: exit 2, `error: the argument '--json' cannot be used multiple times`) | OPEN: amend the wrong clause to the golden (source: `docs/reviews/self-containment-1/NOTES.md`) | |
| OQ-099 | Which RFC 3339 spellings does the loader accept beyond the captured ones: a space instead of `T`, more than nine fraction digits, a leap second `:60`, year `0000`? `port/core/time.bend` accepts `T`/`t`, `Z`/`z`, one to nine fraction digits, seconds up to 59, years from 0001, and refuses the rest as a verdict. | S4.57, S2.24–S2.42 | (case to add: `edge_time_spellings_list_json` on a fixture `timespell` with one record per spelling; a refused record shows how the loader reports it) | OPEN | |
| OQ-100 | Clause contradiction: S4.52 says a mutating command reads the wall clock ONCE per attempt and uses that one instant for every timestamp it writes; S8.31 says one invocation observes several instants (a fresh reading for `updated_at`, another for each edge or comment). Under the pinned clock both predict the same bytes. | S4.52, S8.31 | not capturable under a frozen clock: needs an advancing fake clock (`FAKETIME=@…`), which makes ids nondeterministic; decide by reading the pinned source in a second extraction pass, and state the port's single reading as a DISC if S8.31 holds | OPEN | |
| OQ-101 | What does the original print when `.beads/issues.jsonl` is not valid UTF-8 (a lone 0xFF byte; an overlong form; an encoded surrogate)? The port refuses with its OWN words (`Configuration error: .beads/issues.jsonl is not valid UTF-8`, exit 7) and its decoder does not yet refuse overlong forms, surrogates or scalars past U+10FFFF. | S2.24, S9 | (case to add: `error_store_not_utf8` on a fixture `notutf8` holding the byte 0xFF; the fixture tooling writes text today, so it needs a binary fixture path) | OPEN | |
| OQ-102 | serde_json's messages for STRUCTURAL errors and wrong value shapes. The corpus pins one (`EOF while parsing a string at line 1 column 40`). The port emits serde's wording for `expected value`, `expected `:``, `expected `,` or `}``, `key must be a string`, `trailing comma`, `trailing characters`, `EOF while parsing an object`/`a value`, `invalid escape`, `control character (\u0000-\u001F) found while parsing a string`, `lone leading surrogate in hex escape`, but the columns and the exact texts are unverified; for a wrong shape (`"priority":"high"`, a missing `id`) it emits its own short words (`invalid type: expected an integer`, `missing field `id``) where serde says more (`invalid type: string "high", expected i32 at line 1 column N`). | S2.27, S2.28 | (cases to add: one fixture per message, e.g. `error_json_trailing_comma`, `error_json_wrong_type_priority`, `error_json_missing_id`, `error_json_lone_surrogate`; each `["@fx=<name>","list"]`) | OPEN | |
| OQ-103 | A non-UTF-8 or unreadable `.beads/issues.jsonl`, and a `.beads/` directory WITHOUT `issues.jsonl`: the port's workspace probe opens `./.beads/issues.jsonl` and treats a failure as "not initialized"; the original finds the `.beads` DIRECTORY (walking upward, honoring `BEADS_DIR` and `redirect`). | S2.1–S2.9, S8.12–S8.16 | (cases to add: `where_from_subdir` needs a ws-run directive that sets the working directory below the workspace; `list_beads_dir_without_store` needs a fixture selector that creates `.beads/` with no store) | OPEN | |
| OQ-104 | Which values does `--format` list on `show`, `stats`, `ready`, `blocked` and `dep list`, and is `dep tree --direction` an enumerated option? The port assumes `text, json, toon` and an enumerated direction. | S1.78 | (cases to add: `usage_enum_format_show` `["show","--format","bogus"]`, and the same on `stats`, `ready`, `blocked`, `dep list`; `usage_enum_tree_direction` `["dep","tree","x","--direction","sideways"]`) | RESOLVED from `usage_enum_format_show`, `_stats`, `_ready`, `_blocked`, `_dep_list` (each `[possible values: text, json, toon]`) and `usage_enum_tree_direction` (`[possible values: down, up, both]`): both assumptions hold; S1.78 amended | 2026-09-20 |
| OQ-105 | Does a refused enumerated value that is similar to a listed one earn a tip line, and with which words? The port prints `  tip: a similar value exists: 'status'` after the possible values. | S1.79 | (case to add: `usage_enum_similar_value` `["count","--by","statu"]`) | RESOLVED from `usage_enum_similar_value`: the tip is `  tip: a similar value exists: 'status'`, after the list and a blank line; S1.79 amended; law `cli_similar_value` | 2026-09-20 |
| OQ-106 | The similarity threshold is judged in 64-bit floating point by the original and in exact fractions by the port. Is there a pair (typed word, candidate) among the port's names whose exact similarity is 0.7 or within one rounding step of it? | S1.80 | enumerate the fractions m/la + m/lb + (m−t)/m = 2.1 for lb ≤ 19 and capture one case per hit | OPEN | |
| OQ-107 | When two long names are equally similar to an unknown option, which one is suggested? The port suggests the last in declaration order (own options, aliases after their option, globals, `help`, `version`). | S1.81 | (case to add: a typed name equidistant from two options, e.g. `["list","--lon"]` against `--long`/`--json`) | OPEN | |
| OQ-108 | What does a group print when it is given options but no subcommand (`dep --json`)? The port prints the S1.16 shape for that level (`error: 'br dep' requires a subcommand but one was not provided`, the subcommand list with aliases, the `[OPTIONS]` usage line). | S1.83 | (case to add: `usage_dep_flag_only` `["dep","--json"]`) | RESOLVED from `usage_dep_flag_only`: the S1.16 shape with `'br dep'`, the list `add, import, remove, rm, list, tree, cycles, help` and the `[OPTIONS]` usage line; S1.83 amended | 2026-09-20 |
| OQ-109 | In an exclusion, is X (the argument named first) the first in ARGV order or the one that declares the exclusion? All four captured cases give both readings the same answer. | S1.74 | (case to add: `usage_conflict_wrap_reversed` `["@fx=basic","show","proj-170","--no-wrap","--wrap"]`) | RESOLVED from `usage_conflict_wrap_reversed`: argv order (`'--no-wrap' cannot be used with '--wrap'`); S1.74 amended | 2026-09-20 |
| OQ-110 | `help <command>` (the `help` subcommand followed by words): the port prints the top-level help whatever follows. | S1.89 | (case to add: `usage_help_subcommand_list` `["help","list"]`) | RESOLVED from `usage_help_subcommand_list`: `help list` prints `list`'s long help (3698 bytes); the port follows level words after `help`; S1.89 amended; law `cli_help_forms` | 2026-09-20 |
| OQ-111 | `ready --assignee` without a value against `ready --assignee ""`: the port records both as the empty value. Does the original tell them apart? | S1.49 | (cases to add: `ready_assignee_bare`, `ready_assignee_empty` on `@fx=basic`) | OPEN | |
| OQ-112 | Which of `update`'s and `q`'s options accept a value that begins with `-`? The port gives `-d/--description` that property on `create`, `q` and `update`, `--acceptance-criteria` and `--prerequisites` on `create` only, `-r/--reason` on `close` and `reopen`, `-m/--message` on `comments add`. | S1.36, S1.38, S1.39, S1.87 | (cases to add: `update_description_dash` `["@fx=basic","update","proj-mta","-d","-x"]`, `q_description_dash`) | RESOLVED for `-d`: `usage_update_description_dash` and `usage_q_description_dash` show `-d -x` taken as a value on `update` and `q`. The other options of `update` stay as assumed (no leading `-`) until a case says otherwise | 2026-09-20 |
| OQ-113 | A once-only option with a value given twice at one level (`list --limit 1 --limit 2`): the port refuses it as S1.12 does a flag, spelled `'--limit <LIMIT>'`. | S1.12 | (case to add: `usage_limit_twice`) | RESOLVED from `usage_limit_twice`: refused exactly as assumed; S1.91 | 2026-09-20 |
