{
  "error": {
    "code": "VALIDATION_FAILED",
    "message": "Validation failed: status: refusing to close via `br update --status closed`: terminal-state transitions must go through `br close` so close-policy (close-reason / AC / attribution) is enforced. Use `br close <id> --reason \"...\"` instead, or `br close <id> --bypass-policy --bypass-reason \"...\"` to opt out explicitly. See https://github.com/Dicklesworthstone/beads_rust/issues/301.",
    "hint": null,
    "retryable": true,
    "context": {
      "field": "status",
      "reason": "refusing to close via `br update --status closed`: terminal-state transitions must go through `br close` so close-policy (close-reason / AC / attribution) is enforced. Use `br close <id> --reason \"...\"` instead, or `br close <id> --bypass-policy --bypass-reason \"...\"` to opt out explicitly. See https://github.com/Dicklesworthstone/beads_rust/issues/301."
    }
  }
}
